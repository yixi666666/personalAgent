# ============================================================
# 工具服务 (CapabilityService) 基础配置
# ============================================================

server:
  host: "0.0.0.0"
  port: 8003

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
